import React from 'react';

// if data can change, then usually will not put inside context,
// will pass through provider

const StudentContext = React.createContext({

})

export default StudentContext